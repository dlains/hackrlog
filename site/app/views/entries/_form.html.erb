<%= form_for(@entry, remote: true) do |f| %>
  <% if @entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@entry.errors.count, "error") %> prohibited this entry from being saved:</h2>

      <ul>
      <% @entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <fieldset>
    <%= f.label :content, "Note Text" %>
    <%= f.text_area :content, :rows => 5, :cols => 80, :placeholder => 'Note text.', :accesskey => 'E' %>
  </fieldset>
  <fieldset>
    <%= label_tag :tags, "Tags" %>
    <%= text_field_tag :tags, tag_values(@entry), :size => 103, :placeholder => 'Tags, comma separated.', :accesskey => 'T' %>
  </fieldset>

  <% unless @entry.id? %>
    <%= submit_tag 'Create Note' %>
    <%= f.submit 'Reset', :type => 'reset' %>
  <% else %>
    <%= submit_tag 'Update Note' %>
  <% end %>
<% end %>
